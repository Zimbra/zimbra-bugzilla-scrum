<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <!-- li class="active"><a href="#">Link <span class="sr-only">(current)</span></a></li -->
        {{#if model.prev}}
        <li role="presentation">
          {{#link-to 'sprint' model.prev}}{{model.prev.name}}{{/link-to}}
        </li>
        {{/if}}
        <li role="presentation" class="active">
          {{#link-to 'sprint' model}}Sprint {{model.name}}{{/link-to}}
        </li>
        {{#if model.next}}
        <li role="presentation">
          {{#link-to 'sprint' model.next}}{{model.next.name}}{{/link-to}}
        </li>
        {{/if}}
      </ul>
      <!-- form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form -->
    </div>
    
    <ul class="nav nav-pills navbar-right">
      <li role="presentation" {{bind-attr class="controller.isOverview:active"}}>{{#link-to 'sprint.index'}}Overview{{/link-to}}</li>
      <li role="presentation" {{bind-attr class="controller.isPlan:active"}}>{{#link-to 'sprint.plan'}}Plan{{/link-to}}</li>
      <li role="presentation" {{bind-attr class="controller.isWork:active"}}>{{#link-to 'sprint.work'}}Work{{/link-to}}</li>
      <!-- li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Dropdown <span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu">
          <li><a href="#">Action</a></li>
          <li><a href="#">Another action</a></li>
          <li><a href="#">Something else here</a></li>
          <li class="divider"></li>
          <li><a href="#">Separated link</a></li>
        </ul>
      </li -->
    </ul>
  </div>
</nav>


{{outlet}}
